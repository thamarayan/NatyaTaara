<div class="container-fluid cf-courseRegister">
    <div class="row">
        <div class="col-md-12 col-12">
            <h1>COURSE REGISTER</h1> <a href="/backstage/dashboard" style="text-decoration: none; color: white;">Back to Dashboard</a>
        </div>
        <div class="col-md-12 col-12">
            <% if (course.length > 0) { %>

                <!-- Bollywood Cinematic Freestyle B&C -->
                    <h6 style="text-align: left;">Bollywood / Cinematic Freestyle - Basics & Choreography</h6>
                    <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                        <thead>
                          <tr>
                            <th scope="col">Student</th>
                            <!-- <th scope="col">Age</th>
                            <th scope="col">Contact</th> -->
                            <th scope="col">Course Enrolled</th>
                            <th scope="col">Level</th>
                            <th scope="col">Course Type</th>
                            <th scope="col">Fees Status</th>
                            <th scope="col">Update</th>
                            <th scope="col">Screen</th>
                            <th scope="col">Remove</th>
                          </tr>
                        </thead>
                        <tbody>
                           
                        <% course.forEach(course => { %>
                            <% if (course.course.title === 'Bollywood / Cinematic Freestyle' && course.course.level === 'Basics & Choreography') { %>
                          <tr> 
                            <td><%= course.studentName %></td>
                            <!-- <td><%= course.studentAge %></td>
                            <td><%= course.contact %></td> -->
                            <td><%= course.course.title %></td>
                            <td><%= course.course.level %></td>
                            <td><%= course.course.classType %></td>
                            <td>
                                <% if (course.feesStatus) { %>
                                 Paid
                                 <% } else { %>
                                 Pending
                                <% } %>
                            </td>
                            <td>
                                <% if (course.feesStatus) { %>
                                <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                                <% } else { %>
                                <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                                <% } %>
                            </td>
                            <td>
                                <% if (course.studentStatus) { %>
                                    <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                                 <% } else { %>
                                    <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                                <% } %>
                            </td>
                            <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>     
                          </tr>
                          <% } %>
                          <% }) %>                  
                        </tbody>
                      </table>

                <!-- Bollywood Cinematic Freestyle Choreo -->
                <h6 style="text-align: left;">Bollywood / Cinematic Freestyle - Choreography</h6>
                <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                    <thead>
                      <tr>
                        <th scope="col">Student</th>
                        <!-- <th scope="col">Age</th>
                        <th scope="col">Contact</th> -->
                        <th scope="col">Course Enrolled</th>
                        <th scope="col">Level</th>
                        <th scope="col">Course Type</th>
                        <th scope="col">Fees Status</th>
                        <th scope="col">Update</th>
                        <th scope="col">Screen</th>
                        <th scope="col">Remove</th>
                      </tr>
                    </thead>
                    <tbody>
                       
                    <% course.forEach(course => { %>
                        <% if (course.course.title === 'Bollywood / Cinematic Freestyle' && course.course.level === 'Choreography Class') { %>
                      <tr> 
                        <td><%= course.studentName %></td>
                        <!-- <td><%= course.studentAge %></td>
                        <td><%= course.contact %></td> -->
                        <td><%= course.course.title %></td>
                        <td><%= course.course.level %></td>
                        <td><%= course.course.classType %></td>
                        <td>
                            <% if (course.feesStatus) { %>
                             Paid
                             <% } else { %>
                             Pending
                            <% } %>
                        </td>
                        <td>
                            <% if (course.feesStatus) { %>
                            <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                            <% } else { %>
                            <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                            <% } %>
                        </td>
                        <td>
                            <% if (course.studentStatus) { %>
                                <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                             <% } else { %>
                                <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                            <% } %>
                        </td>
                        <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>          
                      </tr>
                      <% } %>
                      <% }) %>                  
                    </tbody>
                  </table>

                   <!-- Bharathanatyam - Basics -->
                <h6 style="text-align: left;">Bharathanatyam - Basics</h6>
                <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                    <thead>
                      <tr>
                        <th scope="col">Student</th>
                        <!-- <th scope="col">Age</th>
                        <th scope="col">Contact</th> -->
                        <th scope="col">Course Enrolled</th>
                        <th scope="col">Level</th>
                        <th scope="col">Course Type</th>
                        <th scope="col">Fees Status</th>
                        <th scope="col">Update</th>
                        <th scope="col">Screen</th>
                        <th scope="col">Remove</th>
                      </tr>
                    </thead>
                    <tbody>
                       
                    <% course.forEach(course => { %>
                        <% if (course.course.title === 'Bharathanatyam' && course.course.level === 'Basics') { %>
                      <tr> 
                        <td><%= course.studentName %></td>
                        <!-- <td><%= course.studentAge %></td>
                        <td><%= course.contact %></td> -->
                        <td><%= course.course.title %></td>
                        <td><%= course.course.level %></td>
                        <td><%= course.course.classType %></td>
                        <td>
                            <% if (course.feesStatus) { %>
                             Paid
                             <% } else { %>
                             Pending
                            <% } %>
                        </td>
                        <td>
                            <% if (course.feesStatus) { %>
                            <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                            <% } else { %>
                            <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                            <% } %>
                        </td>
                        <td>
                            <% if (course.studentStatus) { %>
                                <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                             <% } else { %>
                                <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                            <% } %>
                        </td>
                        <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>               
                      </tr>
                      <% } %>
                      <% }) %>                  
                    </tbody>
                  </table>


                   <!-- Bollywood / Cinematic Freestyle - Choreography - Intermediate -->
                   <h6 style="text-align: left;">Bollywood / Cinematic Freestyle | Choreography - Intermediate</h6>
                   <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                       <thead>
                         <tr>
                           <th scope="col">Student</th>
                           <!-- <th scope="col">Age</th>
                           <th scope="col">Contact</th> -->
                           <th scope="col">Course Enrolled</th>
                           <th scope="col">Level</th>
                           <th scope="col">Course Type</th>
                           <th scope="col">Fees Status</th>
                           <th scope="col">Update</th>
                           <th scope="col">Screen</th>
                           <th scope="col">Remove</th>
                         </tr>
                       </thead>
                       <tbody>
                          
                       <% course.forEach(course => { %>
                           <% if (course.course.title === 'Bollywood / Cinematic Freestyle' && course.course.level === 'Choreography - Intermediate') { %>
                         <tr> 
                           <td><%= course.studentName %></td>
                           <!-- <td><%= course.studentAge %></td>
                           <td><%= course.contact %></td> -->
                           <td><%= course.course.title %></td>
                           <td><%= course.course.level %></td>
                           <td><%= course.course.classType %></td>
                           <td>
                               <% if (course.feesStatus) { %>
                                Paid
                                <% } else { %>
                                Pending
                               <% } %>
                           </td>
                           <td>
                               <% if (course.feesStatus) { %>
                               <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                               <% } else { %>
                               <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                               <% } %>
                           </td>
                           <td>
                               <% if (course.studentStatus) { %>
                                   <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                                <% } else { %>
                                   <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                               <% } %>
                           </td>
                           <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>                    
                         </tr>
                         <% } %>
                         <% }) %>                  
                       </tbody>
                     </table>

                <!-- Bollywood / Cinematic Freestyle - Choreography - Advanced -->
                <h6 style="text-align: left;">Bollywood / Cinematic Freestyle | Choreography - Advanced</h6>
                <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                    <thead>
                      <tr>
                        <th scope="col">Student</th>
                        <!-- <th scope="col">Age</th>
                        <th scope="col">Contact</th> -->
                        <th scope="col">Course Enrolled</th>
                        <th scope="col">Level</th>
                        <th scope="col">Course Type</th>
                        <th scope="col">Fees Status</th>
                        <th scope="col">Update</th>
                        <th scope="col">Screen</th>
                        <th scope="col">Remove</th>
                      </tr>
                    </thead>
                    <tbody>
                       
                    <% course.forEach(course => { %>
                        <% if (course.course.title === 'Bollywood / Cinematic Freestyle' && course.course.level === 'Choreography - Advanced') { %>
                      <tr> 
                        <td><%= course.studentName %></td>
                        <!-- <td><%= course.studentAge %></td>
                        <td><%= course.contact %></td> -->
                        <td><%= course.course.title %></td>
                        <td><%= course.course.level %></td>
                        <td><%= course.course.classType %></td>
                        <td>
                            <% if (course.feesStatus) { %>
                             Paid
                             <% } else { %>
                             Pending
                            <% } %>
                        </td>
                        <td>
                            <% if (course.feesStatus) { %>
                            <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                            <% } else { %>
                            <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                            <% } %>
                        </td>
                        <td>
                            <% if (course.studentStatus) { %>
                                <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                             <% } else { %>
                                <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                            <% } %>
                        </td>
                        <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>                         
                      </tr>
                      <% } %>
                      <% }) %>                  
                    </tbody>
                  </table>
             
                <!-- Bharathanatyam - Beginners | Basics -->
                <h6 style="text-align: left;">Bharathanatyam - Beginners | Basics</h6>
                <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                    <thead>
                      <tr>
                        <th scope="col">Student</th>
                        <!-- <th scope="col">Age</th>
                        <th scope="col">Contact</th> -->
                        <th scope="col">Course Enrolled</th>
                        <th scope="col">Level</th>
                        <th scope="col">Course Type</th>
                        <th scope="col">Fees Status</th>
                        <th scope="col">Update</th>
                        <th scope="col">Screen</th>
                        <th scope="col">Remove</th>
                      </tr>
                    </thead>
                    <tbody>
                       
                    <% course.forEach(course => { %>
                        <% if (course.course.title === 'Bharathanatyam - Beginners' && course.course.level === 'Basics') { %>
                      <tr> 
                        <td><%= course.studentName %></td>
                        <!-- <td><%= course.studentAge %></td>
                        <td><%= course.contact %></td> -->
                        <td><%= course.course.title %></td>
                        <td><%= course.course.level %></td>
                        <td><%= course.course.classType %></td>
                        <td>
                            <% if (course.feesStatus) { %>
                             Paid
                             <% } else { %>
                             Pending
                            <% } %>
                        </td>
                        <td>
                            <% if (course.feesStatus) { %>
                            <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                            <% } else { %>
                            <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                            <% } %>
                        </td>
                        <td>
                            <% if (course.studentStatus) { %>
                                <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                             <% } else { %>
                                <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                            <% } %>
                        </td>
                        <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>                              
                      </tr>
                      <% } %>
                      <% }) %>                  
                    </tbody>
                  </table>

                <!-- Bharathanatyam - Advanced -->
                <h6 style="text-align: left;">Bharathanatyam - Advanced</h6>
                <table class="table table-responsive table-striped table-dark " style="color:white; text-align: left; margin: 2% 0%;">
                    <thead>
                      <tr>
                        <th scope="col">Student</th>
                        <!-- <th scope="col">Age</th>
                        <th scope="col">Contact</th> -->
                        <th scope="col">Course Enrolled</th>
                        <th scope="col">Level</th>
                        <th scope="col">Course Type</th>
                        <th scope="col">Fees Status</th>
                        <th scope="col">Update</th>
                        <th scope="col">Screen</th>
                        <th scope="col">Remove</th>
                      </tr>
                    </thead>
                    <tbody>
                       
                    <% course.forEach(course => { %>
                        <% if (course.course.title === 'Bharathanatyam - Advanced' && course.course.level === 'ADVANCED') { %>
                      <tr> 
                        <td><%= course.studentName %></td>
                        <!-- <td><%= course.studentAge %></td>
                        <td><%= course.contact %></td> -->
                        <td><%= course.course.title %></td>
                        <td><%= course.course.level %></td>
                        <td><%= course.course.classType %></td>
                        <td>
                            <% if (course.feesStatus) { %>
                             Paid
                             <% } else { %>
                             Pending
                            <% } %>
                        </td>
                        <td>
                            <% if (course.feesStatus) { %>
                            <button class="btn btn-sm btn-success disabled"> Fees Received</button>
                            <% } else { %>
                            <a role="button" href="/backstage/feesUpdate/<%=course._id%>" class="btn btn-sm btn-warning"> Fees Received? </a>
                            <% } %>
                        </td>
                        <td>
                            <% if (course.studentStatus) { %>
                                <a role="button" href="/backstage/block/<%=course._id%>" class="btn btn-sm btn-danger"> Block? </a>
                             <% } else { %>
                                <a role="button" href="/backstage/revoke/<%=course._id%>" class="btn btn-sm btn-warning"> Revoke? </a>
                            <% } %>
                        </td>
                        <td><a role="button" href="/backstage/remove/<%=course._id %>" class="btn btn-danger btn-sm">Remove</a></td>                                   
                      </tr>
                      <% } %>
                      <% }) %>                  
                    </tbody>
                  </table>












            <% } else { %>
               <p style="color: yellow; padding: 5%;">Registrations about to happen</p>
            <% } %>
        </div>
    </div>
</div>  